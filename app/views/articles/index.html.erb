<h1>ARTICULOS INTERESANTES</h1>

<h3><%= link_to "Create", new_article_path %></h3>

  <% @articles.each do |article| %>
    <h2><%= link_to article.title, article_path(article) %></h2> <p><%= link_to "Delete", article_path(article), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} %></p>
    <p><%= article.content %></p>
  <% end %>
